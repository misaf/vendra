<div class="p-6">
    <ul class="list-disc list-inside space-y-4 text-start text-sm text-gray-600 dark:text-gray-400">
        <li>
            <span class="font-semibold text-gray-800 dark:text-gray-100">{{ __('shetab::deposit.transfer_limit_title') }}:</span>
            <p class="ml-2">{{ __('shetab::deposit.transfer_limit_desc') }}</p>
        </li>
        <li>
            <span class="font-semibold text-gray-800 dark:text-gray-100">{{ __('shetab::deposit.information_security_title') }}:</span>
            <p class="ml-2">{{ __('shetab::deposit.information_security_desc') }}</p>
        </li>
        <li>
            <span class="font-semibold text-gray-800 dark:text-gray-100">{{ __('shetab::deposit.high_purchase_support_title') }}:</span>
            <p class="ml-2">{{ __('shetab::deposit.high_purchase_support_desc') }}</p>
        </li>
    </ul>
</div>
